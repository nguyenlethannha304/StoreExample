import { CloseButtonDirective } from './close-button.directive';

describe('CloseButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseButtonDirective();
    expect(directive).toBeTruthy();
  });
});

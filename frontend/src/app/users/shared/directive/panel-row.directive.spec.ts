import { PanelRowDirective } from './panel-row.directive';

describe('PanelRowDirective', () => {
  it('should create an instance', () => {
    const directive = new PanelRowDirective();
    expect(directive).toBeTruthy();
  });
});
